# Copie o ob_discord inteiro para os resources, incluindo bridge.secret.cfg.
# Execute a configuracao secreta ANTES do ensure (ajuste o caminho ao mover).
exec resources/[OB]/ob_discord/bridge.secret.cfg
ensure yarn
ensure webpack
ensure screenshot-basic
ensure ob_discord

# A whitelist inicia desativada para nao bloquear todos antes da configuracao do bot.
# Depois dos testes, altere a convar no bridge.secret.cfg para true.
